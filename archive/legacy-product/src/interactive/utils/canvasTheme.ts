import { IX_COLORS } from "@/interactive/interactiveTheme";

/** Dark immersive canvas palette — re-exports IX_COLORS for canvas draw calls. */
export const GRID = IX_COLORS.grid;
export const AXIS = IX_COLORS.axes;
export const LABEL = IX_COLORS.labels;
export const BACKGROUND = "#0b0910";
